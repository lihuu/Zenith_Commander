
* 更多文件系统支持
** DONE 支持 sftp
sftp 访问的时候，图标显示不正确
** TODO 支持 ftp
** TODO 支持 smb
当前 smb 会直接使用 finder 打开
** TODO 支持第三方网盘
1. Google drive
2. OneDrive
** DONE 如果当前目录是一个软链接，会在 finder 中打开，而不是在当前的 pane 中打开
DEADLINE: <2025-12-18 Thu> SCHEDULED: <2025-12-18 Thu>


   
* 多层级文件搜索
可以集成 fzf 这个工具
* 插件化改造
集成外部工具的代码从主体代码中剥离，进行插件化改造。
* 体验优化
** DONE 拖动文件到目录的时候，鼠标拖动放置到目标目录的时候，应该高亮对应的目录，移动的时候取消高亮
** DONE 书签栏那里的图标，也需要修改默认的图标，这个和 pane 那边的图标应该保持一致。
** DONE 键鼠操作的时候，要支持，mac 默认的复制，粘贴，这种默认的操作。
** DONE 进入目录的时候，不需要总是高亮某一行，比如现在的效果是默认选择第一行 .. ，如果使用键盘，例如 j，k，默认从序号为 1 的进行高亮
** KILL 如果 pane 中存在网络存储的，右键菜单添加额外的菜单移动到左边，或者移动到右边这种，其实就是上传和下载，本地文件系统的时候，是否需要支持呢。
当前拖动可以实现，先不做这个
** DONE 右键菜单的空白要变得更大一些，只有在当前选中的文件上面进行右键菜单，才显示对应文件/目录的右键菜单，否则要应该是父级目录的右键菜单。
** TODO 亮色模式的高亮的颜色需要重新设计一下，现在很难看。
** DONE 文件的移动，复制，删除支持多步骤撤销。
** DONE 目录中的内容，如果是列表模式，支持排序，例如按照名字，按照文件大小，按照修改时间进行排序
** DONE filter 模式 输入问题
1. 内容后面添加光标闪烁吧
2. 当前删除字符没有生效
** DONE command 模式输入问题
1. 内容后面添加光标闪烁吧
2. 当前删除字符没有生效
3. 支持 help 命令，help 或者? 命令可以弹窗显示所有支持的命令
   - ls
** TODO UI 交互优化
   







